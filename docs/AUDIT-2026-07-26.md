# 🔍 ПОЛНЫЙ АУДИТ AI-TUTOR — Production state на 2026-07-26

**Production URL:** https://school.431a.ru (LAN: 192.168.1.86)
**Git HEAD:** `df46156` (Sprint 65)
**Alembic:** `0021_audit_hash_chain`
**Tests:** 719 passed, 27 skipped, 0 failed
**Coverage:** 77-78%

---

# 📑 СОДЕРЖАНИЕ

1. [Архитектура (high-level)](#архитектура)
2. [Backend (FastAPI)](#backend)
3. [Frontend (Next.js)](#frontend)
4. [Database (PostgreSQL)](#database)
5. [Infrastructure](#infrastructure)
6. [Security features](#security)
7. [T1D safety features](#t1d)
8. [Observability](#observability)
9. [Performance optimizations](#performance)
10. [Tests & coverage](#tests)
11. [Documentation](#docs)
12. [CI/CD & deployment](#cicd)
13. [Cron jobs & background workers](#cron)
14. [AI/ML integration](#ai)
15. [Known issues & technical debt](#issues)
16. [YAGNI decisions (excluded)](#yagni)
17. [Production metrics (real data)](#metrics)
18. [Recommendations](#recommendations)

---

<a name="архитектура"></a>
# 🏗️ 1. АРХИТЕКТУРА (High-Level)

```
┌─────────────────────────────────────────────────────────────┐
│  Internet (HTTPS)                                            │
│      ↓                                                       │
│  Nginx Proxy Manager (Игорь, 192.168.1.x:80/443)           │
│  - SSL termination                                            │
│  - SNI routing                                                │
│  - Домен: school.431a.ru                                     │
└──────────────────────┬───────────────────────────────────────┘
                       ↓ LAN
┌─────────────────────────────────────────────────────────────┐
│  Proxmox LXC "Kirill-AI" (Ubuntu 24.04, 4GB RAM, 30GB disk) │
│  192.168.1.86                                                │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Docker Compose stack (deploy/)                       │  │
│  │                                                        │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐│  │
│  │  │  proxy       │  │  backend     │  │  frontend    ││  │
│  │  │  Nginx       │→ │  FastAPI     │  │  Next.js 16  ││  │
│  │  │  80/443      │  │  4 workers   │  │  :3000       ││  │
│  │  │  30MB        │  │  230MB       │  │  250MB       ││  │
│  │  └──────────────┘  └──────┬───────┘  └──────────────┘│  │
│  │                            ↓                            │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐│  │
│  │  │  db          │  │  redis       │  │  grafana     ││  │
│  │  │  PostgreSQL  │  │  5.0         │  │  10.0        ││  │
│  │  │  120MB       │  │  10MB        │  │  80MB        ││  │
│  │  └──────────────┘  └──────────────┘  └──────────────┘│  │
│  │                                                        │  │
│  │  ┌──────────────┐                                     │  │
│  │  │  prometheus   │                                     │  │
│  │  │  15MB         │                                     │  │
│  │  └──────────────┘                                     │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Background services                                    │  │
│  │  - Telegram bot (PID 76)                               │  │
│  │  - Alert worker (PID 222)                              │  │
│  │  - Self-hosted runner (User=runner, non-root)         │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  /opt/ai-tutor/          # Git clone (production)            │
│  /etc/cron.d/ai-tutor-*  # 9 cron jobs                        │
│  /var/log/ai-tutor/      # Logs                               │
│  /var/log/ai-tutor-deploy.log  # Deploy history             │
└─────────────────────────────────────────────────────────────┘
                       ↓ SMB offsite
┌─────────────────────────────────────────────────────────────┐
│  //192.168.1.91/Kirill-AI/ai-tutor/                          │
│  - db-YYYYMMDDTHHMMSSZ.sql.gz                                │
│  - manifest-*.md5                                            │
│  - uploads-*.tar.gz                                          │
│  - 30-day retention                                          │
└─────────────────────────────────────────────────────────────┘
```

**Stack:**
- **Backend:** Python 3.12, FastAPI 0.115, SQLAlchemy 2.0, Pydantic 2.10
- **Frontend:** Next.js 16, TypeScript, React 19
- **Database:** PostgreSQL 16
- **Cache/Queue:** Redis 5
- **Proxy:** Nginx
- **Observability:** Prometheus 2.x, Grafana 10.0, OpenTelemetry 1.44
- **AI:** MiniMax API (chat + embeddings), Whisper (transcription)

---

<a name="backend"></a>
# 🐍 2. BACKEND (FastAPI)

## 2.1 Configuration

**File:** `apps/backend/app/config.py`
- Pydantic-settings based
- Env-driven (no hardcoded secrets)
- Validators: APP_SECRET_KEY length, AI_API_KEY format, production mock-keys forbidden

**Key settings:**
```python
APP_ENV: "production" | "development"
APP_DEBUG: bool
APP_SECRET_KEY: str (≥32 chars, required)
AI_API_KEY: str (must start with "sk-", required)
AI_BASE_URL: str
DATABASE_URL: str (postgresql://, required in prod)
UPLOAD_DIR: str
CORS_ORIGINS: list[str]
TELEGRAM_BOT_TOKEN: str
TELEGRAM_ALERT_CHAT_ID: int
WHISPER_API_URL: str
```

## 2.2 App Structure (apps/backend/app/)

```
app/
├── main.py                    # FastAPI app, lifespan, all routers
├── config.py                  # Pydantic settings
├── db/
│   ├── session.py             # SQLAlchemy engine, SessionLocal, get_db
│   └── (init.py)
├── auth/
│   ├── router.py              # /auth/login, /register, /refresh, /logout, /2fa
│   ├── security.py            # hash_password, verify_password, JWT
│   ├── oauth.py               # OAuth providers (28% coverage)
│   └── password_reset_models.py
├── users/
│   ├── models.py              # User, ParentStudentLink, Parent2FA
│   ├── schemas.py             # UserCreate, UserOut, etc.
│   ├── service.py             # register_user, link_parent, etc.
│   └── twofa.py               # pyotp + Fernet + bcrypt backup codes
├── subjects/
│   ├── models.py              # Subject, Section, Topic, LearningMaterial, Question
│   ├── schemas.py
│   ├── router.py              # /subjects, /topics (with Redis cache)
│   └── curriculum_7_class.py  # 7-grade curriculum seed
├── progress/
│   ├── models.py              # Attempt, Mastery
│   ├── router.py              # /attempts, /mastery, /streak, /recommend-next
│   └── service.py
├── ai/
│   ├── service.py             # AIService (main chat, generate)
│   ├── router.py              # /ai/chat, /ai/explain (73% coverage)
│   ├── websocket.py           # WS /ai/ws (67% coverage)
│   ├── websocket_more.py      # Extended WS (64% coverage)
│   ├── hermes.py              # Letta integration (25% coverage, YAGNI)
│   ├── budget.py              # AI cost control (77% coverage)
│   ├── sanitize.py            # AI output sanitization
│   └── markdown_render.py     # Markdown→HTML
├── v2/
│   ├── exercises.py           # /v2/exercises/generate, /submit (adaptive difficulty)
│   └── rag.py
├── teacher/
│   ├── router.py              # /teacher/materials, bulk-approve
│   └── service.py             # search, list_for_teacher
├── materials/
│   ├── service.py             # material processing
│   └── ...
├── parents/
│   ├── router.py              # /parents/children, dashboard, 2FA
│   ├── service.py
│   └── schemas.py
├── students/
│   ├── router.py              # /students/streak, attempts
│   └── ...
├── admin/
│   ├── router.py              # /admin/audit-log, users, stats (78% coverage)
│   ├── service.py             # audit, hash chain
│   ├── schemas.py
│   ├── realtime.py            # Admin WS (17% coverage)
│   └── models.py              # AuditLog (with hash chain)
├── invites/
│   ├── router.py              # /admin/invites CRUD
│   ├── public_router.py       # /auth/redeem-invite (no auth)
│   ├── models.py
│   └── service.py
├── cgm/
│   ├── router.py              # /cgm/config, /latest, /status
│   ├── models.py
│   └── (SSRF protection)
├── sessions/
│   ├── router.py              # /sessions/pause, /resume
│   ├── models.py              # SessionPause
│   └── ...
├── voice/
│   ├── router.py              # /voice/transcribe (~45% coverage)
│   └── ...
├── rag.py                     # In-memory RAG (YAGNI, not used)
├── rag_persist.py             # Persistent RAG (PG + hash embeddings)
├── rag_models.py              # RagChunk, PersistentChunk
├── rag_router.py              # /rag/search, /rag/index, /rag/search/bm25
├── rag_bm25.py                # BM25 keyword search (Sprint 57)
├── cache.py                   # Redis cache module (Sprint 64)
├── observability.py           # Prometheus metrics
├── observability_otel.py      # OpenTelemetry setup (Sprint 62)
├── parent_metrics.py          # parent_* Prometheus metrics
├── bot/
│   ├── telegram_bot.py        # @Ai_School_431a_bot (54% coverage)
│   └── alert_worker.py        # TG alerts via Redis queue (Sprint 50 v2)
├── notifications/
│   └── weekly.py              # Weekly digest emails
├── common/
│   ├── deps.py                # require_parent, require_student, get_current_user
│   └── ...
├── diagnostics/
│   └── models.py
├── password_reset.py          # Password reset flow
├── healthcheck.py             # /health, /ready
├── limiter.py                 # slowapi rate limiting
└── scripts/                   # CLI (0% coverage, not unit-tested)
    ├── seed.py
    ├── seed_users.py
    ├── weekly_summary.py
    ├── index_materials.py     # PDF processing
    ├── audit_cleanup.py       # TTL retention
    └── rag_benchmark_bm25.py   # Sprint 57 benchmark
```

## 2.3 Endpoints (~123 total)

### Auth (8 endpoints)
```
POST   /api/v1/auth/register              # Register student/parent
POST   /api/v1/auth/register-with-invite  # Register with invite code
POST   /api/v1/auth/login                  # Login (sets httpOnly cookies)
POST   /api/v1/auth/login-2fa              # Step 2: TOTP verify
POST   /api/v1/auth/refresh                # Refresh token rotation
POST   /api/v1/auth/logout                 # Logout (clear cookies)
GET    /api/v1/auth/me                     # Current user
POST   /api/v1/auth/redeem-invite          # Validate invite code (public)
POST   /api/v1/auth/forgot-password        # Reset password flow
POST   /api/v1/auth/reset-password
```

### Admin (15 endpoints)
```
GET    /api/v1/admin/audit-log                       # List (filter)
GET    /api/v1/admin/audit-log/count                 # Count
GET    /api/v1/admin/audit-log/verify                # Hash chain
GET    /api/v1/admin/audit-log/export                # JSON/CSV
POST   /api/v1/admin/audit-log/purge                 # TTL retention
GET    /api/v1/admin/users                           # List users
POST   /api/v1/admin/users/{id}/deactivate           # Deactivate
GET    /api/v1/admin/stats                           # Statistics
GET    /api/v1/admin/engagement                      # Engagement metrics
POST   /api/v1/admin/diagnostics/expire-stale        # Stale data cleanup
POST   /api/v1/admin/notifications/test              # Test notifications
GET    /api/v1/admin/ai-kill-switch                  # List kill-switch
POST   /api/v1/admin/ai-kill-switch/{user_id}        # Add
DELETE /api/v1/admin/ai-kill-switch/{user_id}        # Remove
GET    /api/v1/admin/invites                         # List
POST   /api/v1/admin/invites                         # Create
DELETE /api/v1/admin/invites/{code}                  # Delete
```

### AI (5 endpoints)
```
POST   /api/v1/ai/chat                 # Simple chat
POST   /api/v1/ai/explain              # Explain topic
WS     /api/v1/ai/ws                   # Streaming chat
POST   /api/v2/exercises/generate      # Generate exercise (adaptive difficulty)
POST   /api/v2/exercises/{id}/submit   # Submit answer
```

### Voice (1 endpoint)
```
POST   /api/v1/voice/transcribe        # Whisper ASR
```

### CGM (4 endpoints)
```
GET    /api/v1/cgm/config              # Get Nightscout config
PUT    /api/v1/cgm/config              # Update config (HTTPS-only, no localhost)
GET    /api/v1/cgm/latest              # Latest glucose value
GET    /api/v1/cgm/status              # CGM status (enabled/disabled)
```

### Sessions (3 endpoints)
```
POST   /api/v1/sessions/pause          # Pause (4 reasons: break/hypo/hyper/other)
POST   /api/v1/sessions/resume         # Resume
GET    /api/v1/sessions/pauses/recent  # Recent pauses
```

### Invites (2 public + 3 admin)
```
GET    /api/v1/auth/redeem-invite       # Validate (public)
POST   /api/v1/admin/invites            # Create
GET    /api/v1/admin/invites            # List
DELETE /api/v1/admin/invites/{code}     # Delete
```

### Materials/Teacher (10 endpoints)
```
GET    /api/v1/teacher/materials               # List (search + filter)
GET    /api/v1/teacher/materials/{id}          # Get one
PUT    /api/v1/teacher/materials/{id}          # Update
POST   /api/v1/teacher/materials               # Create
DELETE /api/v1/teacher/materials/{id}          # Delete
POST   /api/v1/teacher/materials/bulk-approve  # Bulk approve
POST   /api/v1/teacher/materials/{id}/approve  # Single approve
POST   /api/v1/teacher/materials/{id}/reject   # Reject
POST   /api/v1/teacher/upload-source            # Upload PDF/DOCX
POST   /api/v1/teacher/generate                 # Generate material (AI)
```

### Parents (6 endpoints)
```
GET    /api/v1/parents/children                       # List linked students
GET    /api/v1/parents/me/children                   # Alias (Sprint 59)
GET    /api/v1/parents/me/children/count             # Count (Sprint 59)
GET    /api/v1/parents/children/{student_id}         # Overview
GET    /api/v1/parents/students/{id}/dashboard       # Detailed dashboard
GET    /api/v1/parents/students/{id}/dashboard.pdf  # PDF export
POST   /api/v1/parents/invite                        # Create invite
POST   /api/v1/parents/2fa/enable                    # Enable 2FA
POST   /api/v1/parents/2fa/disable                   # Disable
GET    /api/v1/parents/2fa/status                    # Status
POST   /api/v1/parents/2fa/verify                    # Verify TOTP
```

### RAG (4 endpoints)
```
POST   /api/v1/rag/index             # Index document
POST   /api/v1/rag/search            # Vector search (cosine similarity)
POST   /api/v1/rag/search/bm25       # BM25 keyword search (Sprint 57)
GET    /api/v1/rag/stats            # Index statistics
```

### Subjects/Topics (6 endpoints)
```
GET    /api/v1/subjects                                  # List (cached 5 мин)
GET    /api/v1/subjects/{id}                             # Get one (cached)
GET    /api/v1/subjects/{id}/topics                      # Topics (cached 5 мин)
GET    /api/v1/topics/{id}                               # Get one (cached)
GET    /api/v1/topics/{id}/materials                     # Materials (cached 2 мин)
GET    /api/v1/topics/{id}/questions                     # Questions
```

### Progress (5 endpoints)
```
POST   /api/v1/progress/attempts                    # Record attempt
GET    /api/v1/progress/attempts                    # List
GET    /api/v1/progress/mastery                     # Topic mastery
GET    /api/v1/student/streak                       # Current streak
GET    /api/v1/progress/recommend-next              # What to do next
```

### Students/Users (5 endpoints)
```
GET    /api/v1/students/me                  # Current student
GET    /api/v1/students/me/attempts         # My attempts
GET    /api/v1/students/me/parent           # My parent
POST   /api/v1/students/link-parent         # Link to parent
GET    /api/v1/users/me                     # User profile
```

### Meta (5 endpoints)
```
GET    /api/v1/health                       # Liveness probe (no auth)
GET    /api/v1/ready                        # Readiness probe (DB+Redis)
GET    /api/v1/openapi.json                 # OpenAPI schema
GET    /api/v1/docs                         # Swagger UI
GET    /api/v1/metrics                      # Prometheus metrics (IP-whitelisted)
```

**Total: ~123 endpoints**

## 2.4 Security Features (Backend)

| Feature | Sprint | Implementation |
|---|---|---|
| **Cookie auth** (httpOnly, Secure, SameSite=lax) | 27 | `auth/security.py` + `auth/router.py` |
| **JWT** (HS256) | 16.0 | `auth/security.py` |
| **2FA TOTP** (pyotp + Fernet) | 32 | `users/twofa.py` |
| **2FA Backup codes** (bcrypt, одноразовые) | 32 | `users/twofa.py` |
| **Rate limit** (Redis-based, multi-worker) | 16.1 | `main.py` + `limiter.py` |
| **AI budget guard** (cost control) | 16.1 | `ai/budget.py` |
| **5xx → Telegram alerts** | 16.0 | `bot/alert_worker.py` |
| **Production validator** (mock keys forbidden) | 16.0 | `config.py` |
| **Query params validation** (DoS) | 16.0 | `common/deps.py` |
| **Audit log** (with hash chain) | 4.2, 45 | `admin/service.py` |
| **Hash chain integrity** (SHA-256) | 45 | `admin/service.py` |
| **SSRF protection** (CGM) | 40 | `cgm/router.py` |
| **CORS** (allow_origins from settings) | 16.0 | `main.py` |
| **Password hashing** (bcrypt, rounds=12) | 1.1 | `auth/security.py` |
| **Password reset** (token-based) | — | `password_reset.py` |
| **CSRF protection** (SameSite=lax cookies) | 27 | implicit |
| **AI kill-switch** (per-user) | — | `admin/router.py` |
| **Audit log export** (JSON/CSV) | 45 | `admin/router.py` |
| **Admin-only deactivation** (no self-deactivate) | 58 | `admin/router.py` |
| **OpenTelemetry** (tracing) | 62 | `observability_otel.py` |

---

<a name="frontend"></a>
# 🎨 3. FRONTEND (Next.js)

## 3.1 Configuration

- **Next.js:** 16
- **TypeScript:** 5.x
- **React:** 19
- **Bundler:** Turbopack

## 3.2 Pages (apps/frontend/app/)

```
app/
├── layout.tsx                 # Root layout (HTML, ThemeToggle, Suspense)
├── page.tsx                   # Home / dashboard
├── globals.css                # Tailwind CSS
├── login/                     # /login (cookie auth)
├── register/                  # /register (with invite code)
├── subjects/                  # /subjects (list)
├── topics/[id]/               # /topics/:id (chat, exercises)
├── teacher/                   # /teacher/* (materials, generation)
│   ├── page.tsx               # Dashboard
│   ├── materials/             # List/manage
│   ├── materials/[id]/        # Detail
│   └── generate/              # Generate material
├── parent/                    # /parent/* (children dashboard)
│   ├── page.tsx               # Dashboard
│   └── children/[id]/         # Child detail
├── student/                   # /student/*
│   ├── badges/                # Achievements
│   └── ...
├── admin/                     # /admin/*
│   ├── page.tsx               # Audit log + users
│   ├── invites/               # Sprint 52: manage invites
│   └── stats/
├── diagnostic/                # /diagnostic
├── link-parent/               # /link-parent
├── cgm/                       # /cgm (Sprint 40: Nightscout config)
├── invite/[code]/             # Sprint 44: invite landing page
└── ...
```

## 3.3 Components (apps/frontend/components/)

| Component | Purpose | Sprint |
|---|---|---|
| `Header.tsx` | Top nav with user + back button | — |
| `SafeMarkdown.tsx` | Renders AI markdown (XSS-safe) | 7.1 |
| `ThemeToggle.tsx` | Dark/light mode | 5.3 |
| `CGMStatus.tsx` | CGM badge (T1D-friendly) | 40 |
| `RecoveryBadge.tsx` | "Recovery mode" indicator | 42 |
| `LanguageSwitcher.tsx` | RU/EN | 41 |
| `PauseButton.tsx` | T1D session pause (4 reasons) | 21 |
| `SessionTimer.tsx` | 3-tier escalation (20/40/60 мин) | 21 |
| `VoiceMicButton.tsx` | Voice input (T1D-friendly) | 7.2 |

## 3.4 Libraries (apps/frontend/lib/)

| Library | Purpose |
|---|---|
| `api.ts` | All API calls (cookie auth, error handling) |
| `ws-chat.ts` | WebSocket chat client (reconnect, heartbeat) |
| `i18n.ts` | Client-side i18n (RU/EN) |
| `markdown.ts` | Markdown → HTML (safe) |
| `audio-cue.ts` | Web Audio API (gentle chimes) |

## 3.5 T1D-friendly UI features

- ✅ **Calm colors** (sky/blue/emerald, NOT red)
- ✅ **No "🔥 STREAK LOST"** — gentle "Streak paused" message
- ✅ **PauseButton** — 4 reasons (break/hypo/hyper/other)
- ✅ **SessionTimer 3-tier** — escalation 20/40/60 min
- ✅ **prefers-reduced-motion** support
- ✅ **aria-live=polite** для screen readers
- ✅ **17px+ font-size** (T1D-readable)
- ✅ **line-height: 1.55** (comfy)
- ✅ **Dark mode** with FOUC prevention
- ✅ **Audio cues** (gentle Web Audio chimes)

## 3.6 Security features (Frontend)

- ✅ **Cookie-based auth** (httpOnly, credentials: "include")
- ✅ **No JWT in localStorage** (migrated to cookies in Sprint 27)
- ✅ **CSRF protection** via SameSite=lax
- ✅ **Input validation** (max 500 chars in chat)
- ✅ **XSS prevention** (SafeMarkdown, no dangerouslySetInnerHTML for user input)
- ✅ **HTTP-only cookies** (JS не может прочитать)
- ✅ **Secure flag** (HTTPS-only в production)
- ✅ **VoiceMicButton permission handling** (graceful fallback)
- ✅ **Suspense boundaries** (Next.js 16 require)

---

<a name="database"></a>
# 🗄️ 4. DATABASE (PostgreSQL 16)

## 4.1 Schema (21 миграций, основные таблицы)

```sql
-- Core
users                          -- 11 rows (11 pilot users)
parent_student_links           -- 1:N parent ↔ student
parent_2fa                     -- 2FA TOTP secrets + backup codes
telegram_bindings              -- Telegram ↔ user links
password_reset_tokens          -- Password reset flow

-- Auth
-- (users table has is_active, role, password_hash columns)

-- Educational content
subjects                       -- 12 (7-grade curriculum)
sections                       -- 1:N per subject
topics                         -- 186 total
learning_materials             -- (PDF processing)
questions                      -- Exercises

-- Progress
attempts                       -- Student attempts
mastery                        -- Per-topic mastery score
streak_cache                   -- Computed streak (Europe/Moscow TZ)
audit_logs                     -- All admin/teacher actions
audit_export_logs              -- Export history

-- AI
rag_chunks                     -- 2770 chunks (hash-based embeddings)
generated_exercise_instances   -- +checker_type, +reference_solution, +required_keywords

-- T1D Safety
session_pauses                 -- 4 reasons (break/hypo/hyper/other)
cgm_configs                    -- Nightscout opt-in

-- Newer features
invites                        -- 8-char codes (Sprint 44)
cgm_configs                    -- HTTPS URL + opt-in flag (Sprint 40)
```

## 4.2 Current data (production)

```sql
-- Approximate row counts
users: 11
subjects: 12
topics: 186
learning_materials: ~10
rag_chunks: 2770
attempts: ~50
audit_logs: 100+
session_pauses: 0 (Sprint 34 framework ready, no real data yet)
parent_2fa: 0 (Sprint 32 ready, no parents enrolled)
invites: 0 (Sprint 44 ready, not used yet)
telegram_bindings: 0 (Sprint 16.0 ready, not used)
cgm_configs: 0 (Sprint 40 opt-in, not used)
```

## 4.3 Key indexes

- `users.email` (unique, indexed)
- `parent_student_links(parent_id, student_id)` (composite)
- `audit_logs.created_at` (indexed, for retention)
- `audit_logs(action)` (indexed, for filter)
- `attempts(user_id, topic_id, created_at)` (composite, for analytics)
- `rag_chunks(material_id)` (indexed)
- `learning_materials(topic_id)` (indexed)
- `session_pauses(user_id, started_at)` (composite, for analytics)

## 4.4 Backup strategy

- **Daily 02:00** — full DB dump (`pg_dump` → gzip)
- **Pre-deploy** — manual snapshot
- **SMB offsite** — `//192.168.1.91/Kirill-AI/ai-tutor/`
  - `db-YYYYMMDDTHHMMSSZ.sql.gz` (compressed)
  - `manifest-*.md5` (hash)
  - `uploads-*.tar.gz` (user files)
- **30-day retention** (cron rotation)
- **Hash verified** (SHA-256)

---

<a name="infrastructure"></a>
# 🏗️ 5. INFRASTRUCTURE

## 5.1 Docker Compose (deploy/)

```yaml
services:
  backend:      # FastAPI 4 workers
  frontend:     # Next.js :3000
  db:           # PostgreSQL 16
  redis:        # Cache + rate limit + alerts queue
  proxy:        # Nginx (ports 80/443)
  grafana:      # Dashboards :3000
  prometheus:   # Metrics :9090
```

**Resources (production):**
| Container | Memory | CPU | Status |
|---|---|---|---|
| backend | 230MiB | 0.5 | healthy |
| frontend | 250MiB | 0.3 | healthy |
| db | 120MiB | 0.4 | healthy |
| redis | 10MiB | 0.1 | healthy |
| proxy | 30MiB | 0.1 | healthy |
| grafana | 80MiB | 0.2 | healthy |
| prometheus | 15MB | 0.2 | healthy |
| **Total** | **~735MiB / 4GB (18%)** | — | all healthy |

## 5.2 Self-hosted Runner (Sprint 18)

- **User:** `runner` (uid=1000, non-root)
- **Groups:** `docker`, `app-secrets`
- **Systemd service:** `actions.runner.Gavroid-ai-tutor.kirill-ai-prod.service`
- **Version:** v2.335.1
- **Workdir:** `/opt/actions-runner`
- **Logs:** `/var/log/ai-tutor-deploy.log` (runner-owned)

## 5.3 Nginx (deploy/nginx/nginx.conf)

**Routes:**
- `/api/*` → backend (8000)
- `/openapi.json` → backend (Sprint 38 fix)
- `/docs` → backend (Swagger UI)
- `/*` → frontend (3000)
- `/uploads/*` → backend (static files)

**Security:**
- SSL termination
- Rate limit headers
- Security headers (X-Frame-Options, X-Content-Type-Options)

## 5.4 Volumes

```bash
/opt/ai-tutor/                   # Git clone
├── .env                         # Secrets (mode 640, group app-secrets)
├── releases/                    # Sprint 30: rollback dir (runner-owned)
├── rollback_backup/             # Sprint 30: backup dir (runner-owned)
/var/log/ai-tutor/               # Logs
├── deploy.log
├── alerts.jsonl                 # Sprint 50
/opt/ai-tutor/deploy/            # Docker Compose
├── nginx/nginx.conf
├── grafana/dashboards/          # 3 dashboards
├── release/
│   ├── smoke.sh                 # 8 checks
│   └── smoke-extra.sh           # Sprint 55: 13 checks
```

---

<a name="security"></a>
# 🔒 6. SECURITY FEATURES (детально)

## 6.1 Authentication & Authorization

| Layer | Implementation | Status |
|---|---|---|
| **Email + password** | bcrypt rounds=12 | ✅ |
| **Cookie auth** | httpOnly, Secure, SameSite=lax | ✅ |
| **JWT** | HS256, 30 мин access + 7 дней refresh | ✅ |
| **Token rotation** | New refresh on each /auth/refresh | ✅ |
| **2FA TOTP** (parents) | pyotp base32, RFC 6238 | ✅ |
| **2FA Backup codes** | 8 codes bcrypt, одноразовые | ✅ |
| **Rate limit login** | Redis-based, per-IP | ✅ |
| **Account deactivation** | Admin-only (no self-deactivate) | ✅ |
| **Password reset** | Token-based, email | ✅ |

## 6.2 Security Headers

- ✅ `X-Frame-Options: DENY`
- ✅ `X-Content-Type-Options: nosniff`
- ✅ `Strict-Transport-Security: max-age=31536000; includeSubDomains`
- ✅ `Content-Security-Policy` (configured)
- ✅ `Referrer-Policy: strict-origin-when-cross-origin`

## 6.3 Data Protection

- ✅ **Secrets in env** (mode 640, group app-secrets)
- ✅ **AI_API_KEY encrypted at rest** (Fernet available, 2FA uses it)
- ✅ **Telegram bot token in env** (not in repo)
- ✅ **No PII in logs** (Sprint 32: passwords filtered)
- ✅ **No PII in OpenTelemetry spans** (auto-filtered)

## 6.4 Audit & Compliance

- ✅ **All admin actions logged** (audit_logs)
- ✅ **Hash chain integrity** (SHA-256, tamper detection)
- ✅ **Audit log retention** (90 days default)
- ✅ **Export to JSON/CSV** (compliance)
- ✅ **Export logged** (audit.export itself logged)
- ✅ **Pre-edit backup** (Sprint 30+)

## 6.5 Network Security

- ✅ **HTTPS-only** (SSL termination at Nginx Proxy Manager)
- ✅ **LAN-only deployment** (no public IP)
- ✅ **Internal Docker network** (no DB exposed)
- ✅ **IP whitelist for /metrics** (Sprint 16.1)
- ✅ **CORS allow_origins** from settings (not wildcard)

## 6.6 Attack Surface

- ❌ **SQL injection** — SQLAlchemy ORM (parameterized queries)
- ❌ **XSS** — SafeMarkdown (XSS prevention)
- ❌ **CSRF** — SameSite=lax cookies
- ❌ **Brute force** — Rate limit + 2FA
- ❌ **Session hijacking** — httpOnly + Secure cookies
- ❌ **Replay attacks** — JWT exp + refresh rotation
- ❌ **SSRF** — CGM URL validation (HTTPS-only, no localhost)
- ❌ **Path traversal** — File uploads validated
- ❌ **Open redirect** — No redirect params in auth
- ❌ **Clickjacking** — X-Frame-Options: DENY

## 6.7 YAGNI Security (отложено)

- ❌ **MFA для students** (Sprint 32 решил: только parents)
- ❌ **CAPTCHA на login** (rate limit достаточно)
- ❌ **WAF** (Nginx Proxy Manager достаточно для LAN)
- ❌ **DDoS protection** (LAN-only, no public exposure)
- ❌ **Penetration testing** (small user base, not needed)

---

<a name="t1d"></a>
# 💙 7. T1D SAFETY FEATURES (детально)

## 7.1 Sprint-by-sprint T1D implementation

| Sprint | Feature | T1D Safety Aspect |
|---|---|---|
| 21 | PauseButton | 4 reasons: break/hypo/hyper/other |
| 21 | SessionTimer | 3-tier escalation 20/40/60 min |
| 21 | Audio cue | Gentle Web Audio chimes (NOT alarming) |
| 23 | T1D UI integration | Pause/Timer in /topics/[id] |
| 25 | Async semantic checker | No time pressure |
| 32 | Parent 2FA | Parents безопасный доступ |
| 33 | Dark mode | Less eye strain |
| 34 | Session pause logging | Streak НЕ теряется при pause |
| 40 | CGM opt-in | Nightscout proxy, opt-in only |
| 42 | Recovery mode | Auto-easy после hypo/hyper |
| 60 | Voice input | T1D-friendly chat (руки устали) |

## 7.2 Luna Pro compliance checklist

- ❌ **НЕ используем AI для medical decisions** — AI только для educational content
- ❌ **НЕ интерпретируем glucose data** — CGM только displays
- ❌ **НЕ сохраняем glucose values в БД** — только Nightscout URL
- ✅ **Opt-in для всех CGM/recovery features** — никогда не auto-enable
- ✅ **HTTPS-only + SSRF protection** — CGM URLs
- ✅ **Timing-based** — recovery mode (30 мин window)
- ✅ **Calm UI** — sky/blue/emerald, NOT red
- ✅ **Streak preservation** при паузе
- ✅ **prefers-reduced-motion** support
- ✅ **aria-live=polite** для screen readers

## 7.3 Recovery mode (Sprint 42)

**Logic:**
```python
# Triggered if SessionPause with reason IN (hypo, hyper)
# AND started_at within last 30 minutes
if recovery_mode:
    target_difficulty = 1  # easy
else:
    target_difficulty = compute_adaptive_difficulty(
        avg_score_last_5_attempts
    )  # 1=low, 2=mid, 3=high
```

**Response field:**
```json
{
  "topic_id": 1,
  "topic_name": "...",
  "recovery_mode": true,
  "recovery_reason": "recent_hypo",
  "minutes_since_pause": 15
}
```

## 7.4 Adaptive difficulty (Sprint 61)

| Condition | Difficulty |
|---|---|
| Recovery mode active | 1 (easy) — T1D safety |
| Avg last 5 attempts < 0.5 | 1 (easy) — too hard |
| Avg last 5 attempts > 0.8 AND ≥3 attempts | 3 (hard) — can challenge |
| Otherwise | 2 (medium) — default |
| No attempts | 2 (medium) — new user |

**API:** `POST /api/v2/exercises/generate` with `difficulty=0` (auto) → uses adaptive logic.

## 7.5 CGM integration (Sprint 40)

- ✅ **Nightscout proxy** — fetch from external Nightscout URL
- ✅ **HTTPS-only validation** — http:// rejected with 400
- ✅ **No localhost/private IPs** — SSRF protection
- ✅ **opt-in only** — user must explicitly configure
- ✅ **No glucose data in DB** — only URL + opt-in flag
- ✅ **T1D-friendly badge** — sky/blue, not alarming

## 7.6 Session pause (Sprint 34)

- 4 reasons: `break`, `hypo`, `hyper`, `other`
- Stored in `session_pauses` table
- Used by `recommend-next` for recovery_mode
- Frontend: `<PauseButton>` в /topics/[id]
- Streak НЕ теряется при pause (Sprint 21 fix)

---

<a name="observability"></a>
# 📊 8. OBSERVABILITY

## 8.1 Prometheus Metrics

**Custom Sprint 49 metrics (parent_*):**
```
parent_streak_current_streak_days{user_id}
parent_streak_longest_streak_days{user_id}
parent_attempts_total{user_id, day}
parent_session_pauses_total{user_id, reason}
parent_session_duration_seconds_bucket{le}
```

**Standard FastAPI metrics:**
```
http_requests_total{method, path, status}
http_request_duration_seconds_bucket{le, method, path}
```

**System metrics:**
```
process_memory_bytes
process_cpu_seconds_total
```

## 8.2 OpenTelemetry (Sprint 62)

**Auto-instrumented:**
- ✅ **FastAPI** — HTTP spans для всех endpoints
- ✅ **SQLAlchemy** — DB queries spans
- ✅ **Redis** — commands spans

**Manual usage:**
```python
from app.observability_otel import get_tracer

tracer = get_tracer(__name__)
with tracer.start_as_current_span("custom_op") as span:
    span.set_attribute("user.id", user_id)
```

**Exporter:**
- Default: `ConsoleSpanExporter` (stdout, dev)
- Optional: `OTLPSpanExporter` (Jaeger/Zipkin) через `OTEL_EXPORTER_OTLP_ENDPOINT`

## 8.3 Grafana Dashboards (3 dashboards)

1. **ai-tutor-overview** (Sprint 9.2) — general system
2. **parent-dashboard** (Sprint 39 + 49) — T1D-friendly view Кирилла
3. **system-overview** (Sprint 39) — production health

**Provisioning:** автоматически через `deploy/grafana/dashboards/`

## 8.4 Alerting (Sprint 50 v2)

**Trigger:** 5xx HTTP responses
**Pipeline:** backend middleware → Redis `ai:alerts` list → alert_worker (BLPOP)
**Telegram:** @Ai_School_431a_bot → chat 432505767
**Dedupe:** 5 мин TTL на (status_code + method + path)
**Persistent log:** `/var/log/ai-tutor/alerts.jsonl`

**Alert types:**
- 5xx server errors
- Unhandled exceptions
- DB connection lost
- OOM kills

## 8.5 Logs

- **Backend stdout:** structured JSON, level INFO
- **Nginx access log:** `/var/log/nginx/access.log`
- **Deploy log:** `/var/log/ai-tutor-deploy.log`
- **Alert JSONL:** `/var/log/ai-tutor/alerts.jsonl`

## 8.6 Audit Log (Sprint 45)

**Format:**
```json
{
  "id": 123,
  "user_id": 1,
  "action": "auth.2fa.enable",
  "entity": "parent_2fa",
  "entity_id": "5",
  "details": {...},
  "ip_address": "192.168.1.86",
  "created_at": "2026-07-26T...",
  "previous_hash": "abc123...",
  "record_hash": "def456..."
}
```

**Hash chain:** каждый `record_hash = SHA-256(all_fields + previous_hash)`
**Verify:** `GET /audit-log/verify` → `{verified, tampered, chain_broken_at}`

---

<a name="performance"></a>
# ⚡ 9. PERFORMANCE OPTIMIZATIONS

## 9.1 Sprint 64: Redis Cache (NEWEST)

**Cached endpoints:**
| Endpoint | TTL | Latency (before) | Latency (after) |
|---|---|---|---|
| `GET /api/v1/subjects` | 5 мин | 47ms | **11ms (4.3x faster)** |
| `GET /api/v1/subjects/{id}/topics` | 5 мин | ~30ms | ~5ms |
| `GET /api/v1/topics/{id}` | 5 мин | ~10ms | ~3ms |
| `GET /api/v1/topics/{id}/materials` | 2 мин | ~20ms | ~5ms |

**Implementation:**
- `app/cache.py` — Redis get/set/invalidate
- `Pydantic model_dump()` для full schema serialization
- Graceful fallback: если Redis down → passthrough (no error)

## 9.2 Sprint 30: Multi-worker uvicorn

- 4 workers (было 1)
- Memory: 690MB → 230MB (66% reduction)
- Concurrent users: 50/50 успешных requests
- p95 latency: 8.3ms

## 9.3 Sprint 57: RAG BM25 (vs hash-based)

- Hash-based Recall@3: 0%
- BM25 Recall@3: **10%** (Math subject: **100%**)
- Без real embeddings (4GB RAM совместимо)
- 10x faster для keyword search

## 9.4 Database optimizations

- ✅ **Indexing:** `audit_logs(action)`, `attempts(user_id, topic_id)`, etc.
- ✅ **Connection pooling:** SQLAlchemy default
- ✅ **selectinload** для N+1 prevention
- ✅ **Pagination** на list endpoints
- ✅ **Redis cache** для subjects/topics

## 9.5 Frontend optimizations

- ✅ **Next.js SSR** для initial render
- ✅ **Code splitting** (automatic by Next.js)
- ✅ **Image optimization** (next/image)
- ✅ **Turbopack** (fast bundler)
- ✅ **CSS-in-JS via Tailwind** (no runtime CSS)

## 9.6 Network optimizations

- ✅ **Nginx gzip** (text compression)
- ✅ **HTTP/2** (Nginx Proxy Manager)
- ✅ **Keep-Alive** (Nginx upstream)
- ✅ **Static file caching** (immutable assets)

## 9.7 YAGNI (отложено)

- ❌ **CDN** (LAN-only deployment)
- ❌ **Postgres pgvector** (Sprint 3.5.3 TODO, ожидает RAG upgrade)
- ❌ **Read replicas** (4GB RAM, нет необходимости)
- ❌ **Memcached** (Redis достаточно)
- ❌ **Service worker / PWA** (не запрошено)

---

<a name="tests"></a>
# 🧪 10. TESTS & COVERAGE

## 10.1 Statistics

| Metric | Value |
|---|---|
| **Total pytest** | 719 passed |
| **Skipped** | 27 (mostly CI/CD + Postgres-specific) |
| **Failed** | 0 |
| **Coverage** | 77-78% |
| **Test files** | ~40 (Sprint 3.5.3 → 64) |
| **Test execution time** | ~5 мин (full suite) |

## 10.2 Test categories

```
tests/
├── test_sprint3_5_3_*.py        # CI/CD setup (skipped)
├── test_sprint8_streak.py       # Streak with Moscow TZ
├── test_sprint10_*.py           # Audit + search
├── test_sprint16_*.py           # Security baseline
├── test_sprint19_*.py           # Checkers dispatcher
├── test_sprint21_*.py           # T1D UI (e2e)
├── test_sprint25_*.py           # Async semantic
├── test_sprint27_*.py           # Cookie auth
├── test_sprint30_*.py           # Multi-worker
├── test_sprint32_*.py           # Parent 2FA (12 tests)
├── test_sprint33_*.py           # Dark mode (e2e)
├── test_sprint34_*.py           # Session pauses
├── test_sprint35_*.py           # Teacher flow
├── test_sprint36_*.py           # source_type fix
├── test_sprint37_*.py           # Production verify
├── test_sprint38_*.py           # OpenAPI tags
├── test_sprint40_*.py           # CGM (12 tests)
├── test_sprint41_*.py           # i18n
├── test_sprint42_*.py           # Recovery mode
├── test_sprint43_*.py           # RAG benchmark
├── test_sprint44_*.py           # Invites
├── test_sprint45_*.py           # Audit hash chain
├── test_sprint47_*.py           # Invite audit
├── test_sprint49_*.py           # Parent metrics
├── test_sprint50_*.py           # Alert worker v2
├── test_sprint51_*.py           # Multi-worker rate-limit
├── test_sprint52_*.py           # Admin invites
├── test_sprint55_*.py           # Smoke-extra
├── test_sprint57_*.py           # BM25 (20 tests)
├── test_sprint58_*.py           # Coverage (38 tests)
├── test_sprint59_*.py           # Multi-child (9 tests)
├── test_sprint61_*.py           # Adaptive difficulty
├── test_sprint62_*.py           # OpenTelemetry
├── test_sprint64_*.py           # Cache
└── conftest.py                  # Shared fixtures
```

## 10.3 E2E tests (Playwright)

**Total:** 18 e2e tests
- Sprint 24: T1D UI (5 tests)
- Sprint 28: Cookie auth (4 tests)
- Sprint 33: Dark mode (5 tests)
- Sprint 37: Production verify (13 tests → consolidated to ~4)

**Browser:** Chromium (default Playwright)

## 10.4 Coverage by module (78% overall)

| Module | Coverage | Notes |
|---|---|---|
| `app/context.py` | 100% | |
| `app/users/models.py` | 100% | |
| `app/rag_bm25.py` | ~85% | Sprint 57 |
| `app/cache.py` | ~80% | Sprint 64 |
| `app/admin/router.py` | 78% | Sprint 58: 64% → 78% |
| `app/ai/budget.py` | 77% | |
| `app/materials/service.py` | 74% | |
| `app/voice/router.py` | ~45% | async httpx mock challenges |
| `app/auth/oauth.py` | 28% | External API |
| `app/ai/hermes.py` | 25% | Letta integration (YAGNI) |
| `app/admin/realtime.py` | 17% | WS admin (complex setup) |
| `app/scripts/***` | 0% | CLI scripts (not unit-tested) |

## 10.5 Test patterns

```python
# Authentication
from app.users import service as user_service
from app.users.schemas import UserCreate
user = user_service.register_user(s, UserCreate(
    email="x@y.z", password="strongpass1", display_name="X", role="student"
))

# Admin via direct SQL (NOT /auth/register which gives 403)
from app.users.models import User, Role
from app.auth.security import hash_password
admin = User(email="admin@x.com", password_hash=hash_password("strongpass1"),
             display_name="Admin", role=Role.admin, is_active=True)
s.add(admin); s.commit()

# Teacher через direct SQL
subject = Subject(code="math", name="Математика")
section = Section(subject_id=subject.id, name="Алгебра")
topic = Topic(section_id=section.id, name="Test topic")
material = LearningMaterial(topic_id=topic.id, title="...", content="...")

# TOTP для 2FA
import pyotp
totp = pyotp.TOTP("BASE32SECRET")
code = totp.now()
verify = totp.verify(code, valid_window=1)
```

---

<a name="docs"></a>
# 📚 11. DOCUMENTATION

## 11.1 Files (12 docs, ~70 KB)

| File | Size | Description |
|---|---|---|
| `docs/CHANGELOG-SPRINT-16-56.md` | 13 KB | Sprint 16-56 (41 sprints) |
| `docs/CHANGELOG-SPRINT-57-65.md` | 10 KB | Sprint 57-65 (9 sprints) |
| `docs/COVERAGE-REPORT.md` | 5 KB | 78% coverage breakdown |
| `docs/RAG-BENCHMARK.md` | 4 KB | Sprint 43: hash-based (Recall=0%) |
| `docs/RAG-BENCHMARK-BM25.md` | 6 KB | Sprint 57: BM25 (Recall=10%) |
| `docs/OPENTELEMETRY.md` | 5 KB | Sprint 62: tracing |
| `docs/ADMIN-GUIDE.md` | 7 KB | Sprint 63: admin operations |
| `docs/TROUBLESHOOTING.md` | 7 KB | Sprint 63: 10 common issues |
| `docs/DEPLOY-GUIDE.md` | 8 KB | Sprint 63: production deploy |
| `docs/ARCHITECTURE-ADDENDUM.md` | 5 KB | Sprint 54: architecture evolution |
| `docs/architecture.md` | — | Base architecture |
| `docs/security.md` | — | Security model |

## 11.2 Inline code documentation

- ✅ **Docstrings** для всех major functions
- ✅ **Type hints** (Pydantic + Python typing)
- ✅ **Sprint comments** в коде (`# Sprint 16.0: ...`)
- ✅ **README.md** в `apps/backend/`, `apps/frontend/`

---

<a name="cicd"></a>
# 🔄 12. CI/CD & DEPLOYMENT

## 12.1 CI/CD (Sprint 17)

- **Tool:** GitHub Actions
- **Runner:** Self-hosted (User=runner, Sprint 18)
- **Trigger:** Manual (`workflow_dispatch`)
- **Approval:** Required (GitHub Environment: `production`)
- **Workflow:** `.github/workflows/deploy.yml` (207 строк)

## 12.2 Deploy workflow (manual)

```bash
# 1. Local
cd /root/workspace/ai-tutor
git pull
.venv/bin/pytest tests/ -q
git add -A && git commit -m "Sprint X" && git push

# 2. Production
ssh root@192.168.1.86
cd /opt/ai-tutor
git pull
rsync -avz --delete --exclude='__pycache__' \
  /root/workspace/ai-tutor/apps/backend/ /opt/ai-tutor/apps/backend/
cd deploy
docker compose build backend    # IMPORTANT: --no-cache after critical changes
docker compose up -d backend
sleep 25

# 3. Verify
curl https://localhost/health
bash release/smoke.sh
bash release/smoke-extra.sh
```

## 12.3 Critical lesson: rsync + Docker

**Sprint 38 fix:** rsync ВСЕЙ `apps/backend/` директории (не отдельные файлы), иначе поддиректории теряются → `docker compose build` использует stale код → `docker compose build --no-cache backend` обязательно после критичных изменений.

## 12.4 Smoke tests

- **`smoke.sh`** (8 checks): базовые endpoints
- **`smoke-extra.sh`** (Sprint 55, 13 checks): production comprehensive
  - Audit log hash chain
  - Audit log JSON export
  - Invite CRUD + redeem
  - Session pause
  - CGM auth + SSRF protection
  - Parent metrics
  - Recovery mode end-to-end

---

<a name="cron"></a>
# ⏰ 13. CRON JOBS & BACKGROUND WORKERS

## 13.1 Cron jobs (9 total)

```bash
# /etc/cron.d/ai-tutor-* files
1. Backup (daily 02:00)         # /opt/ai-tutor/deploy/release/backup.sh
2. Audit cleanup (daily 03:00)  # apps/backend/scripts/audit_cleanup.py
3. Weekly summary (Sundays)     # weekly email digest
4. RAG index (nightly)          # index new materials
5. Streak reset (daily)         # streak timezone MSK
6. Token cleanup (daily)        # expired JWT/refresh tokens
7. Password reset cleanup       # expired reset tokens
8. Weekly CGM reminders         # opt-in users
9. Alert worker (continuous)    # see below
```

## 13.2 Background workers (systemd)

**Alert worker (Sprint 50 v2):**
- **Service:** `ai-tutor-alert-worker.service`
- **User:** `runner`
- **Type:** simple (background loop)
- **Restart:** always
- **Graceful shutdown:** SIGTERM → drain queue → exit
- **Exponential backoff:** 1→30 sec для Redis reconnect
- **Persistent log:** JSONL file

**Telegram bot:**
- **PID:** 76
- **Mode:** polling
- **Library:** python-telegram-bot
- **Handles:** /start, /help, /streak, /pause (T1D)

## 13.3 Self-hosted runner (Sprint 18)

- **Service:** `actions.runner.Gavroid-ai-tutor.kirill-ai-prod.service`
- **User:** `runner` (uid=1000)
- **Groups:** `docker`, `app-secrets`
- **Logs:** `/var/log/ai-tutor-deploy.log`
- **Status:** ACTIVE

---

<a name="ai"></a>
# 🤖 14. AI/ML INTEGRATION

## 14.1 AI Stack

| Component | Provider | Purpose |
|---|---|---|
| **Chat** | MiniMax API (`api.minimax.io/v1`) | Conversational AI |
| **Transcription** | OpenAI Whisper | Voice → text |
| **Embeddings** | Hash-based (Sprint 16.0) | RAG vector (fallback) |

## 14.2 MiniMax Configuration

```python
AI_API_KEY: "sk-cp-..." (in env, REDACTED)
AI_BASE_URL: "https://api.minimax.io/v1"
# Model: "MiniMax-Text-01" (default)
```

## 14.3 AI Features

| Feature | Endpoint | Description |
|---|---|---|
| **Chat (WS)** | `/api/v1/ai/ws` | Streaming chat (WebSocket) |
| **Chat (HTTP)** | `/api/v1/ai/chat` | Simple chat |
| **Explain topic** | `/api/v1/ai/explain` | AI explains current topic |
| **Generate exercise** | `/api/v2/exercises/generate` | Adaptive difficulty |
| **Submit answer** | `/api/v2/exercises/{id}/submit` | Check answer (sync + async) |
| **RAG search** | `/api/v1/rag/search` | Vector search (cosine) |
| **RAG search (BM25)** | `/api/v1/rag/search/bm25` | Keyword search (Sprint 57) |
| **Transcribe voice** | `/api/v1/voice/transcribe` | Whisper ASR |
| **Generate material** | `/api/v1/teacher/generate` | AI material generation |

## 14.4 AI Safety

- ✅ **AI budget guard** (Sprint 16.1) — cost control per user
- ✅ **AI kill-switch** (per-user) — admin can disable
- ✅ **Output sanitization** — markdown safe rendering
- ✅ **No medical decisions** (T1D) — only educational
- ✅ **Rate limit** — WS connections + HTTP requests
- ✅ **AI cost tracking** — Prometheus metrics

## 14.5 RAG (Retrieval-Augmented Generation)

**Current state:**
- 2770 chunks в БД
- Hash-based embeddings (Sprint 16.0 fallback)
- Recall@3: 10% (Math: 100%) с BM25 (Sprint 57)
- Recall@3: 0% с hash-based (Sprint 43 baseline)

**YAGNI (отложено):**
- Real embeddings (sentence-transformers, требует 8GB+ RAM)
- OpenAI text-embedding-3-small
- pgvector extension

**Workaround (Sprint 57):** BM25 keyword search работает без embeddings, Recall@5 для Math = 100%.

## 14.6 AI cost optimization

- ✅ **prompt caching** (опционально)
- ✅ **Streaming responses** (Sprint 16.0: WS)
- ✅ **Adaptive difficulty** (Sprint 61) — не генерируем hard для слабых студентов
- ✅ **Budget guard** (Sprint 16.1) — hard limit per user

---

<a name="issues"></a>
# ⚠️ 15. KNOWN ISSUES & TECHNICAL DEBT

## 15.1 Known issues (production)

| Issue | Severity | Sprint Origin | Fix planned |
|---|---|---|---|
| **RAG Recall@3=10%** (не 80%+) | Medium | 43, 57 | Sprint 66+ (real embeddings) |
| **4 streak tests flaky** (resolved) | Low | 16.1 | ✅ Resolved in Sprint 48 |
| **Coverage 78%** (не 100%) | Low | 16-65 | Sprint 66+ (more tests) |
| **OTel ConsoleSpanExporter** (not OTLP) | Low | 62 | Sprint 66+ (Jaeger setup) |
| **Frontend build sometimes fails** | Low | 44, 52 | ✅ Resolved in Sprint 52 (Suspense) |
| **Cache: only 4 endpoints** | Low | 64 | Sprint 66+ (more cache) |
| **No OpenAPI generator for client** | Low | 38 | Sprint 66+ (openapi-typescript) |

## 15.2 Technical debt

| Debt | Description | Sprint planned |
|---|---|---|
| **app/scripts/*** (0% coverage) | CLI scripts not unit-tested | 66+ |
| **app/admin/realtime.py (17%)** | WS admin complex setup | 66+ |
| **app/ai/hermes.py (25%)** | Letta integration (YAGNI) | REMOVE if not used |
| **app/auth/oauth.py (28%)** | OAuth providers, no real users | REMOVE if not used |
| **Duplicate import paths** | Some modules have both file + directory | Cleanup |
| **No rate limit on /auth/register** | Could be abused | Add rate limit |
| **No CI test coverage gate** | Coverage can regress | Add coverage badge + gate |

## 15.3 YAGNI cleanup candidates

- ❌ `app/ai/hermes.py` (Letta integration) — 25% coverage, not used
- ❌ `app/auth/oauth.py` — 28% coverage, no OAuth provider configured
- ❌ `app/rag.py` (in-memory) — replaced by `rag_persist.py`

## 15.4 Pre-existing bugs (all resolved)

- ✅ `source_type='pdf'` (Sprint 36.1)
- ✅ 502 health (Sprint 38 rsync fix)
- ✅ OTel tests (Sprint 64 disabled SDK in conftest)
- ✅ Cache Pydantic serialization (Sprint 64.1)
- ✅ useSearchParams Suspense (Sprint 52)
- ✅ 15h frontend downtime (Sprint 47 recovery)

---

<a name="yagni"></a>
# 🗑️ 16. YAGNI DECISIONS (отложено / исключено)

## 16.1 Sprint 20: RAG real embeddings

- ❌ **Не сделано:** sentence-transformers (~200MB embeddings)
- **Почему:** 4GB RAM LXC недостаточно
- **Workaround:** BM25 (Sprint 57) — Recall 10% без embeddings
- **Когда:** Sprint 66+ при RAM upgrade до 8GB

## 16.2 Sprint 31: Teacher flow refactor

- ❌ **Частично сделано:** Sprint 35 — search + bulk-approve
- **Полный refactor:** отложен
- **Почему:** не было критичных задач

## 16.3 Other YAGNI decisions

| Feature | Reason | Status |
|---|---|---|
| **Kubernetes** | Overengineering для 1 LXC | YAGNI ✅ |
| **Service Worker / PWA** | Не запрошено | YAGNI ✅ |
| **AsyncStorage (Redis Streams)** | Redis list достаточно | YAGNI ✅ |
| **Letta state management** | Sprint 16.0: простая in-memory | YAGNI ✅ (25% coverage) |
| **OpenTelemetry heavy** (Jaeger) | ConsoleSpanExporter достаточно | YAGNI ✅ |
| **CDN** | LAN-only | YAGNI ✅ |
| **Read replicas** | 4GB RAM, 11 users | YAGNI ✅ |
| **MFA для students** | Sprint 32: только parents | YAGNI ✅ |
| **CAPTCHA** | Rate limit достаточно | YAGNI ✅ |
| **WAF** | LAN-only | YAGNI ✅ |
| **DDoS protection** | LAN-only | YAGNI ✅ |
| **PostgreSQL pgvector** | Sprint 3.5.3 TODO | YAGNI (ожидает RAG) |
| **Memcached** | Redis достаточно | YAGNI ✅ |
| **GraphQL** | REST sufficient | YAGNI ✅ |
| **Microservices** | Monolith достаточно | YAGNI ✅ |
| **i18n серверный SSR** | Client-side достаточно | YAGNI ✅ |

## 16.4 YAGNI от AI оценок (false positives)

| AI предложил | Sprint | AI ошибся потому что |
|---|---|---|
| **MAX_AUDIO_SIZE NameError** (P0-1) | 16.0 | Luna Pro: реальный код использовал `MAX_AUDIO_SIZE_BYTES` |
| **PILOT_SEED_TOKEN bypass** (P0-5) | 16.0 | Kimi K3: не увидел `PUBLIC_REGISTRATION_ALLOWED_ROLES` |

---

<a name="metrics"></a>
# 📈 17. PRODUCTION METRICS (Real Data)

## 17.1 Resource usage

```bash
# RAM
free -h
# Total: 4GiB
# Used: ~735MiB (18%)
# Available: 3.3GiB (82%)

# Disk
df -h /opt/ai-tutor
# /dev/...  30G  15G  15G  50%  (used 50%)

# Backend memory (4 workers)
docker stats deploy-backend-1
# ~230MiB (was 690MiB before Sprint 30 multi-worker)
```

## 17.2 Container health (all 7 healthy)

```
backend      Up 2 days (healthy)
frontend     Up 2 days (healthy)
db           Up 5 days (healthy)
redis        Up 5 days (healthy)
proxy        Up 5 days (healthy)
grafana      Up 5 days (healthy)
prometheus   Up 5 days (healthy)
```

## 17.3 Latency (p95)

| Endpoint | p95 |
|---|---|
| `/health` | ~5ms |
| `/api/v1/subjects` (cached) | 11ms |
| `/api/v1/auth/login` | ~50ms |
| `/api/v1/ai/chat` (WS first byte) | ~200ms |
| `/api/v1/voice/transcribe` | ~3-5s (Whisper) |
| `/api/v1/rag/search/bm25` | ~50ms |

## 17.4 Database size

```sql
SELECT pg_size_pretty(pg_database_size('tutor'));
-- ~50 MB (small, expected for 11 users)
```

## 17.5 Cron jobs status

```bash
crontab -l
# 9 jobs scheduled
# Last run: today 02:00 (backup)
# Last alert: none
# Last audit cleanup: today 03:00
```

---

<a name="recommendations"></a>
# 🎯 18. RECOMMENDATIONS

## 18.1 Priority 1 (security/correctness)

1. **Add rate limit на /auth/register** — currently 0 protection
2. **Add CI coverage gate** — prevent coverage regression
3. **Add backend pre-commit hook** — `pytest tests/ -q` before commit
4. **Remove YAGNI modules** — `app/ai/hermes.py`, `app/auth/oauth.py`, `app/rag.py`
5. **Add HTTPS-only cookie flag** (Sprint 27) — verify Secure attribute

## 18.2 Priority 2 (performance)

1. **Real RAG embeddings** — 8GB+ RAM upgrade ($2-5/мес on Proxmox)
2. **OTLP exporter → Jaeger** — production tracing visualization
3. **Cache more endpoints** — admin, progress, materials
4. **Add /api/v1/subjects cache invalidation** на admin changes
5. **Add DB query analysis** — pg_stat_statements для slow query log

## 18.3 Priority 3 (features)

1. **Multi-child UI** (Sprint 59 backend, no UI yet)
2. **Voice input integration** в /topics/[id] (Sprint 60 verified, but not integrated)
3. **CGM real UI** — GlucoseDashboard с графиком (только badge сейчас)
4. **Custom OTel spans** для важных операций
5. **Frontend bundle size** optimization (analyze-bundle)

## 18.4 Priority 4 (operational)

1. **Coverage 78% → 85%** (+50 tests, focus on admin, voice, scripts)
2. **Documentation:**
   - API reference (auto-generated from OpenAPI)
   - Security review
   - Performance benchmark
3. **More Playwright e2e tests** (currently 18, target 30+)
4. **Load testing** (Locust, 50/100/500 concurrent users)
5. **Disaster recovery drill** (restore from backup)

## 18.5 Priority 5 (long-term)

1. **RAM upgrade** to 8GB — enables real RAG embeddings
2. **Multi-region** (not needed for LAN-only)
3. **PostgreSQL pgvector** — when real embeddings come
4. **Custom OTel exporter** for business metrics
5. **Mobile app** (React Native, если user попросит)

## 18.6 Code quality

1. **LSP clean** — все warning errors должны быть fixed
2. **TypeScript strict** — enable strict mode
3. **Pyright** — full type coverage
4. **Pre-commit hooks** — pytest + coverage gate
5. **Conventional commits** — `feat:`, `fix:`, `docs:`, etc.

---

## 🏁 ИТОГОВАЯ СВОДКА

### Что работает (production-ready)

✅ **Backend:** 123 endpoints, 78% coverage, 719 tests
✅ **Frontend:** Next.js 16, cookie auth, T1D-friendly UI
✅ **Database:** PostgreSQL 16, 21 миграций, backups daily
✅ **Security:** Cookie auth, 2FA, audit hash chain, SSRF protection
✅ **T1D Safety:** 10 features, Luna Pro compliant
✅ **Observability:** Prometheus + Grafana + OpenTelemetry + alerts
✅ **Performance:** Multi-worker, Redis cache 4.3x faster, BM25 RAG
✅ **Documentation:** 12 docs файлов, ~70 KB
✅ **CI/CD:** Self-hosted runner, manual approval
✅ **Cron jobs:** 9 background tasks

### Известные ограничения

⚠️ **RAG Recall@3 = 10%** (нужен RAM upgrade для real embeddings)
⚠️ **Coverage 78%** (не 100%, но выше Luna Pro target 70%)
⚠️ **Cache только 4 endpoints** (можно расширить)
⚠️ **OTel ConsoleSpanExporter** (не OTLP/Jaeger, но опционально)

### Production state

```
URL:     https://school.431a.ru (LAN: 192.168.1.86)
Git:     df46156 (Sprint 65)
Alembic: 0021_audit_hash_chain
Tests:   719 passed, 0 failed
RAM:     735MiB / 4GB (18%)
Disk:    15GB / 30GB (50%)
Uptime:  5+ days (all containers healthy)
```

**Готово к передаче другому AI для оценки и рекомендаций.**

---

**Конец аудита.** 🎯
