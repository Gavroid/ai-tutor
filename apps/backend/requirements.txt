# Backend runtime dependencies (locked to working versions as of 2026-07).
# Production: rebuild with --no-cache in Docker. Dev: `pip install -r requirements.txt -r requirements-dev.txt`.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
prometheus-client==0.21.1  # Sprint 5.1 — метрики /metrics
pydantic-settings==2.7.0

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10
asyncpg==0.30.0

# Auth (Sprint 3.39: passlib → bcrypt-direct).
# passlib 1.7.4 выдавал deprecation warning "module 'bcrypt' has no attribute
# '__about__'" при каждом вызове hash/verify. Sprint 3.39 заменил на прямые
# вызовы bcrypt.hashpw/checkpw с тем же контрактом ($2b$12$..., salt,
# совместим со старыми хэшами в БД).
bcrypt==4.0.1
python-jose[cryptography]==3.5.0  # Sprint continuation 2026-08-23: 5 CVEs pinned to 3.4.0+, applied 3.5.0 (jose.utcnow internal patched upstream).
python-multipart==0.0.20

# HTTP client (для AI Gateway)
httpx==0.28.1

# Utilities
python-dotenv==1.2.2  # Sprint continuation 2026-08-23: PYSEC-2026-2270 (CVE-2026-...).
email-validator==2.2.0

# File processing (Этап 10)
pypdf==5.1.0
python-docx==1.1.2
# OCR (Этап 10 — опционально, требует tesseract-ocr в системе)
pytesseract==0.3.13
Pillow==11.0.0

# Rate limiting (Этап 11)
slowapi==0.1.9

# Redis для rate limit (Этап 11, опционально)
redis==5.2.0

# Markdown render (Sprint 7.1)
# Используется для безопасного рендеринга AI-ответов с markdown → HTML.
markdown-it-py==3.0.0

# WebSockets client (Sprint 9.3 — admin WS smoke; уже нужен фронтом на prod-tools)
websockets==13.1
# 2FA TOTP (Sprint 32 — Parent 2FA)
pyotp==2.9.0

# Sprint 62: OpenTelemetry (distributed tracing)
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-sqlalchemy==0.65b0
opentelemetry-instrumentation-redis==0.65b0

# Sprint 70: Real RAG embeddings (8GB RAM unlocked)
# torch/torchvision из PyTorch CPU index (--index-url)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.10.0
torchvision==0.25.0
sentence-transformers==5.6.1


# Sprint 91: OpenTelemetry OTLP HTTP exporter (для Jaeger/Zipkin)
opentelemetry-exporter-otlp-proto-http==1.44.0

# Sprint 94: GraphQL для mobile clients
strawberry-graphql==0.253.1
