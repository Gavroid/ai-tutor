# Dev/test-only dependencies. NOT installed in production image.
-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-xdist==3.6.1  # Sprint 3.31: parallel pytest (×3.5 speedup, 9:32 → 2:45)
diff-cover==10.5.1    # Sprint 3.40: coverage gate в PR (diff vs base branch)
httpx==0.28.1
fakeredis==2.26.1

# Sprint 3.22: статические гейты качества (Этап 2.1 / 2.3 плана 13-*).
# ruff — линтер+форматтер (заменяет flake8+isort+black).
# mypy — проверка типов в строгом режиме для /parents и /student.
ruff==0.7.4
mypy==2.3.1

# Sprint 3.43 C: type stubs для mypy strict (требуются в CI).
# types-passlib: stubs для passlib.context (используется в app/auth/security.py).
# aiosmtplib: runtime + stubs для app/notifications/service.py (SMTP).
types-passlib==1.7.7.20260211  # noqa: E501
aiosmtplib>=5.1.2,<6