# Dev/test-only dependencies. NOT installed in production image.
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-xdist==3.6.1  # Sprint 3.31: parallel pytest (×3.5 speedup, 9:32 → 2:45)
httpx==0.28.1
fakeredis==2.26.1

# Sprint 3.22: статические гейты качества (Этап 2.1 / 2.3 плана 13-*).
# ruff — линтер+форматтер (заменяет flake8+isort+black).
# mypy — проверка типов в строгом режиме для /parents и /student.
ruff==0.7.4
mypy==1.13.0